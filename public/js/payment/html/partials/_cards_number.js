'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM card numbers HMTL string
* 
* @return {String} the shadow DOM card numbers HMTL string
* 
*/
export default () => {
    return `<div class="card-number others-cards-number" id="cards-number"></div>`
}