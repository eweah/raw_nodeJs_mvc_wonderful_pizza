'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM name on card HMTL string
* 
* @return {String} the shadow DOM name on card HMTL string
* 
*/
export default () => {
    return `<div class="name-on-card" id="name-on-card"></div>`
}