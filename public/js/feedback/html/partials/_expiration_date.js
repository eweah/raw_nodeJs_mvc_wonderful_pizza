'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM card expiration dates HMTL string
* 
* @return {String} the shadow DOM card expiration dates HMTL string
* 
*/
export default () => {
    return `<div class="card-expiration-date" id="card-expiration-date"></div>`
}