'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM action buttons HMTL string
* 
* @return {String} the shadow DOM action buttons HMTL string
* 
*/
export default () => {
    return `<fieldset class="action-buttons" id="action-buttons"></fieldset>`
}