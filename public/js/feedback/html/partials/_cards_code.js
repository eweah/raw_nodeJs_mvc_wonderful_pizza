'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM card codes HMTL string
* 
* @return {String} the shadow DOM card codes HMTL string
* 
*/
export default () => {
    return `<div class="security-code others-card-code" id="cards-code"></div>`
}