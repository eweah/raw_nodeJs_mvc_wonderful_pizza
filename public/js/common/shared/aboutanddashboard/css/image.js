'use strict'
/**
* @name  anonymous (no name)
* @function
* 
* @description  holds the shadow DOM image styling string
* 
* @return {String} the shadow DOM image styling string
* 
*/
export default () => {
    return `.fakeimg {
        background-color: #aaa;
        width: 100%;
        padding: 20px;
    }`
}