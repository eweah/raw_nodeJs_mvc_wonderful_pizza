'use strict'

/**
   * @name  anonymous (no name)
   * @function
   * 
   * @description holds the shadow DOM image HTML string
   * 
   * @return {String} the shadow DOM image HTML string 
   * 
   */
export default () => {
    return `
    .fakeimg {
        background-color: #aaa;
        width: 100%;
        padding: 20px;
    }
`
}