'use strict'
/**
   * @name  anonymous (no name)
   * @function
   * 
   * @description holds the shadow image styling string
   * 
   * @return {String} the shadow image styling string 
   * 
   */
export default () => {
    return `.fakeimg {
        background-color: #aaa;
        width: 100%;
        padding: 20px;
    }`
}